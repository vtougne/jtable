
select:
  - as: my hostname
    expr: host.hostname
  - as: os
    expr: host.os
  - as: state
    expr: host.state
  - as: city
    expr: city.name
  # - as: region
  #   expr: region.key
