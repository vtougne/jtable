vars:
  status_matrix:
    SU: Success
    FA: Failed

sources:
  host_data:
    type: file
    path: 

select:
  - as: region
    expr: region.key
  - as: dc
    expr: dc.key
  - as: my hostname
    expr: host.hostname
  - as: os
    expr: host.os
  - as: state
    expr: host.state
