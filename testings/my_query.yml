queryset:
  path: simple{}
  select: "hostname,os"
  # select:
  # - as: hostname
  #   expr: hostname
  # - as: os
  #   expr: os
  # - as: cost
  #   expr: cost
out: '{{ stdin | jtable(queryset=queryset) }}'

