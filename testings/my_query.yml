vars:
  queryset:
    path: simple{}
    select:
    - as: hostname
      expr: hostname
    - as: os
      expr: os
    - as: cost
      expr: cost
    format: json
  prepa_stdout: '{{ stdin | from_json_or_yaml | jtable(queryset=queryset) }}'
stdout: '{{ prepa_stdout | from_json|to_nice_yaml }}'

