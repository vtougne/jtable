- text: "# Setup"      

- name: "pip installer"
  eval_cmd: echo pip install .
  box_as: bash

- text: "# manual install in case of failure"

- name: "pip installer"
  eval_cmd: echo pip install . --no-deps
  box_as: bash


- text: "## Manual install external dependencies"
- name: "pip install tabulate"
  eval_cmd: echo pip install tabulate
  box_as: bash

- name: "pip installer"
  eval_cmd: echo pip install PyYAML
  box_as: bash

- name: "pip Jinja2"
  eval_cmd: echo pip install Jinja2
  box_as: bash


